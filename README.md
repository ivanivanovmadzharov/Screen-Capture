**SCREEN CAPTURE**
<br>Simple ElectronJS desktop app for screen capture
--
**To install the app:**
1. Download the project folder
2. Open the Command Prompt in the folder directory
3. Type *npm install --save-dev electron-builder*
4. Type *npm run build*
5. Open the .exe file in the **dist** folder